/*import { Router } from 'express';
import * as catalogController from '../controllers/catalog.controller.js';

const router = Router();

// Ubicación geográfica (estado, municipio, parroquia)
router.get('/geography', catalogController.getFullGeography); 

// Parámetros de la Ficha Social
router.get('/social-parameters', catalogController.getSocialParameters); 

// Estructura Legal
router.get('/legal-areas', catalogController.getLegalStructure); 

export default router;*/